[color]
    diff = auto
    status = auto
    branch = auto
[merge]
    tool = fugitive
    conflictstyle = diff3
[rerere]
    enabled = true
[core]
    editor = vim
[mergetool "fugitive"]
    cmd = vim -f -c \"Gdiff\" \"$MERGED\"
